export interface Step1Data { branch: string; people: number; }
export interface ProductPickMulti { productIds: string[]; }
export interface ProductPick { productId: string; }
export type Product = { id: string; name: string; priceSar: number };
